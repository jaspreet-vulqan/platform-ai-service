# ----------------------------------------------------------------------------
# Linux + NVIDIA GPU only. vLLM does not run on native Windows (use the Linux
# host, WSL2, or Docker). Install in a Python 3.10-3.12 venv.
#
# We run vLLM's own `vllm serve` as a separate backend process and proxy to it
# over HTTP. Our app does NOT import vllm's internals, so the vLLM version is
# free to move — no pin is required for our code to keep working. Use a version
# that supports your model: 'gemma4' (unsloth/gemma-4-31B-unsloth-bnb-4bit) was
# added in vLLM 0.19.0 and needs transformers >= 5.5.0. See
# https://docs.vllm.ai/projects/recipes for the recommended Gemma 4 version.
# ----------------------------------------------------------------------------
vllm>=0.19.0
transformers>=5.5.0

# Required to load bitsandbytes (bnb) pre-quantized checkpoints such as the
# unsloth gemma-4 bnb-4bit weights.
bitsandbytes>=0.46.0

# Gateway dependencies (our app). fastapi/uvicorn/pydantic also come with vllm;
# httpx is the proxy/client transport.
fastapi
uvicorn[standard]
pydantic
httpx
python-dotenv==1.0.1
